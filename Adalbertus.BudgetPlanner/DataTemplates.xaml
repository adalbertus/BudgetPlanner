<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:cal="http://www.caliburnproject.org"
             xmlns:i="clr-namespace:System.Windows.Interactivity;assembly=System.Windows.Interactivity"
             xmlns:ext="clr-namespace:Adalbertus.BudgetPlanner.Extensions"
             xmlns:extToolkit="http://schemas.microsoft.com/winfx/2006/xaml/presentation/toolkit/extended"
             xmlns:ctrl="clr-namespace:Adalbertus.BudgetPlanner.Controls"
             xmlns:vm="clr-namespace:Adalbertus.BudgetPlanner.ViewModels"
             xmlns:m="clr-namespace:Adalbertus.BudgetPlanner.Models"
                    >
    <DataTemplate DataType="{x:Type m:CashFlow}">
        <WrapPanel>
            <TextBlock Text="{Binding FullName}" Grid.Column="1" FontSize="10" Margin="2"/>
            <Border BorderBrush="LightGray" BorderThickness="1" CornerRadius="4" Grid.Column="0" Margin="2,1,1,1" Padding="2">
                <TextBlock Text="{Binding GroupName}" FontSize="10"/>
            </Border>

        </WrapPanel>
    </DataTemplate>
</ResourceDictionary>